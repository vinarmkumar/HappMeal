{
  "name": "happymeal-backend",
  "version": "1.0.0",
  "description": "Backend for HappyMeal - AI recipe and grocery list generator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:debug": "LOG_LEVEL=DEBUG nodemon server.js",
    "dev:trace": "LOG_LEVEL=TRACE nodemon server.js",
    "logs": "tail -f logs/app-$(date +%Y-%m-%d).log 2>/dev/null || echo 'No log file found for today. Logs are written to logs/app-YYYY-MM-DD.log'",
    "logs:watch": "watch 'tail -20 logs/app-$(date +%Y-%m-%d).log 2>/dev/null || echo \"No log file found\"' .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "recipe",
    "grocery",
    "mern",
    "nodejs",
    "express",
    "mongodb"
  ],
  "author": "Satish Sahu",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.2.1",
    "axios": "^1.10.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
